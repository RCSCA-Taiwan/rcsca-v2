# V1320 Notes

- Closed the SSR auth-cookie CDN cache-header gap.
- Upgraded and pinned the Supabase SSR/client dependencies used by the application.
- Resolved the production dependency audit from two known vulnerabilities to zero without a Next.js major-version jump.
- Reverified all 69 routes and the production Middleware build.
- Production remains unchanged.

