# V570

第一階段建置尾段：MY 1% 真實資料化、共享小隊邀請生命週期與後台小隊管理。

- MY 1% 移除硬編碼姓名、XP、共享點、足跡、小隊與同行天數，改讀 Supabase 真實帳號資料。
- 共享小隊邀請改為 14 天有效的一次性 token；資料庫只保存 SHA-256 token hash。
- 登入流程可保留 invite token，完成 Email OTP 後才接受邀請。
- 介紹來源寫入 invitations 並永久保留，但不影響 XP、點數、會籍或利益。
- 一個使用者同時間只加入一個共享小隊；小隊長不能自行離隊，需先轉移小隊長。
- 新增 /admin/teams，讓管理端建立小隊與指定已註冊帳號為小隊長。
- 通知中心支援 team 相關通知導回 /team。
- 本地相對 import 掃描：0 遺失。
