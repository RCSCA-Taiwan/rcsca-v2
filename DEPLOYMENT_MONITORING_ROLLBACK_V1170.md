# 部署、監控與回復手冊（v1170）

## 部署前

1. 執行 npm run verify，環境變數檢查與 production build 必須通過。
2. 確認六個管理 RPC 已在目標資料庫比對完成；未完成不得開放相關後台功能。
3. 確認部署環境只注入必要金鑰，服務角色金鑰不得出現在瀏覽器 bundle。
4. 建立可回復的前一個應用版本標記，並記錄對應資料庫 migration 版本。

## 健康檢查

- 路徑：/api/health
- 正常：HTTP 200、status=ok、configured=true。
- 降級：缺少公開 Supabase 設定時回傳 HTTP 503。
- 回應不包含 URL、金鑰、帳號或資料庫內容。
- 建議每 5 分鐘檢查一次，連續 3 次失敗才告警，避免短暫部署切換誤報。

## 最低監控

- 可用性：首頁、登入頁、/api/health。
- 錯誤率：5xx、RPC 失敗、登入 callback 失敗。
- 關鍵流程：活動報名、需求送出、企業申請、後台核實。
- 安全：大量登入失敗、權限拒絕異常增加、Audit Log 中不明角色操作。
- 資料：回饋重複扣點、重複足跡、通知異常暴增。

## 回復判定

立即回復前一版本：

- 登入或帳號資料全面失敗。
- 權限越界、敏感資料外洩或非授權管理操作。
- 重複扣點、重複庫存扣除或重複共享足跡。
- 主要流程 5xx 持續超過 10 分鐘。

## 回復步驟

1. 暫停新部署與高風險管理操作。
2. 切回上一個已驗證應用版本。
3. 不自動倒退資料庫 migration；先確認是否為向後相容。
4. 驗證健康檢查、登入及一條唯讀會員流程。
5. 記錄事件時間、影響範圍、版本與處理人。
6. 若涉及資料錯寫，先保全 Audit Log，再依核准程序修復。

## 安全回應標頭

全站加入 nosniff、DENY frame、嚴格來源 Referrer Policy，並停用相機、麥克風與定位權限；未加入可能破壞 Supabase 連線的過度嚴格 CSP。
