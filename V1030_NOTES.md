# V1030 — Account residue audit and CSS compatibility

- Localized redemptions, referral source, and consolidated request progress in zh-Hant, English, Japanese, and Korean.
- Localized statuses, empty states, sign-in boundaries, action links, and locale-aware dates.
- Replaced unsupported CSS flex alignment `end` values with `flex-end` across the global stylesheet.
- Preserved existing user-scoped queries and Supabase behavior; no schema, RPC, or RLS changes.
