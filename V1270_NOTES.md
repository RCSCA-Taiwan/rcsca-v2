# V1270 Notes

- Added a deterministic schema contract for public and private schemas.
- Fingerprints tables, columns, constraints, indexes, views, policies, functions, triggers, relation grants, and routine grants.
- Added a repeatable SQL drift check.
- This detects remote drift but does not replace the pending canonical schema dump.
