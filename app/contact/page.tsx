'use client';
import SiteHeader from '../SiteHeader';import Link from 'next/link';import {useI18n} from '../i18n';import {institutionCopy} from '../institutionCopy';
export default function Contact(){const {locale}=useI18n();const c=institutionCopy[locale].contact;const hrefs=['/care-actions/request','/1percent-partner/esg','/1percent-network','/account/notifications','/account/security','/about'];return <main className="institutionPage"><SiteHeader/><section className="institutionHero compact"><div className="portalWrap"><div className="eyebrow">CONTACT & HELP</div><h1>{c.title}</h1><p>{c.lead}</p></div></section><section className="portalSection"><div className="portalWrap"><div className="portalGrid three">{c.cards.map((x:any,i:number)=><Link href={hrefs[i]} key={hrefs[i]}><small>{x[0]}</small><h3>{x[1]}</h3><p>{x[2]}</p><span>{x[3]}</span></Link>)}</div><div className="privacyCallout"><strong>{c.callout[0]}</strong><p>{c.callout[1]}</p></div></div></section></main>}
