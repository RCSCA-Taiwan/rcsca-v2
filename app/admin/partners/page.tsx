import SiteHeader from '../../SiteHeader';
import {partnerShares, statusLabel} from '../../../lib/mockData';
export default function AdminPartners(){return <main className="adminPage"><SiteHeader/><section className="flowHero"><div className="portalWrap"><div className="eyebrow">企業共享審核</div><h1>先確認用途，再決定它進哪一條共享路徑。</h1><p>公益資源、共享所、會員禮遇、工作機會與一般企業合作分流處理，避免企業提供的 1% 被錯誤轉用。</p></div></section><section className="portalSection"><div className="portalWrap"><div className="stateList">{partnerShares.map(x=><article key={x.id}><div className="stateMeta"><small>{x.shareType}</small><span>{x.id}</span></div><h3>{x.title}</h3><div className="stateRow"><span className={`statusChip status-${x.status}`}>{statusLabel[x.status]}</span><span>{x.publicResult?'成果可公開':'目前不公開'}</span></div></article>)}</div><div className="privacyCallout"><strong>年度標章不是付款取得。</strong><p>企業完成經 RCSCA 核實的實質共享後，才具備相應年度共享紀錄；贊助金額不作前台排序與等級依據。</p></div></div></section></main>}
