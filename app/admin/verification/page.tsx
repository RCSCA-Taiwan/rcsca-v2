import SiteHeader from '../../SiteHeader';
import {pendingParticipations, statusLabel} from '../../../lib/mockData';
export default function AdminVerification(){return <main className="adminPage"><SiteHeader/><section className="flowHero"><div className="portalWrap"><div className="eyebrow">活動完成核實</div><h1>核實的是「有沒有參與」，不是付了多少。</h1><p>現金、轉帳、現場服務等不同交付方式最後都回到同一個完成判定。正式版核實後才寫入共享足跡與 XP 流水。</p></div></section><section className="portalSection"><div className="portalWrap"><div className="verificationTable"><div className="vtHead"><span>活動</span><span>參與方式</span><span>狀態</span><span>共享足跡</span></div>{pendingParticipations.map(x=><div className="vtRow" key={x.id}><span><b>{x.activity}</b><small>{x.id}</small></span><span>{x.participationType}</span><span><i>{statusLabel[x.status]}</i></span><span>{x.footprintCreated?'已建立':'核實後建立'}</span></div>)}</div><div className="privacyCallout"><strong>正式操作會有雙層保護。</strong><p>批次核實、人工更正、撤銷核實等高風險行為必須留下操作者、時間、原因與來源紀錄。</p></div></div></section></main>}
