'use client';
import {useState} from 'react';
import SiteHeader from '../../SiteHeader';
const options=['時間','專業','人脈連結','物資／資源','工作機會','陪伴','分享資訊','還不知道'];
export default function StartMyOne(){const [picked,setPicked]=useState<string[]>([]);const [done,setDone]=useState(false);const toggle=(x:string)=>setPicked(v=>v.includes(x)?v.filter(i=>i!==x):[...v,x]);return <main className="startOnePage"><SiteHeader/><section className="startHero"><div className="portalWrap"><div className="eyebrow">建立 MY 1% · 我的共享</div><h1>不用先想著「我要做多少」，先從「我有什麼」開始。</h1><p>1% 沒有固定單位。它可以是一段時間、一項專業、一個人脈連結、一份資源，甚至只是你願意開始留意身邊的需要。</p></div></section><section className="portalSection"><div className="portalWrap startFlow"><div><small>01 · 我的 1% 可能是</small><h2>先選幾個最接近你的方式。</h2><div className="oneOptions">{options.map(x=><button key={x} onClick={()=>toggle(x)} className={picked.includes(x)?'active':''}>{x}</button>)}</div></div><div className="startSummary"><small>02 · 建立起點</small><h2>{done?'MY 1% 已建立':'你的起點不需要完美。'}</h2><p>{done?`已記下：${picked.length?picked.join('、'):'尚未定義'}。之後可以隨時修改，真正的共享等級只會由實際參與逐步形成。`:'先留下方向，之後再讓真實參與形成你的共享足跡。註冊本身不會讓共享等級自動提高。'}</p><button className="submitLead" onClick={()=>setDone(true)}>{done?'完成 ✓':'建立我的 MY 1% →'}</button>{done&&<a className="textRoute" href="/my-1percent">進入我的共享首頁 →</a>}</div></div></section></main>}
